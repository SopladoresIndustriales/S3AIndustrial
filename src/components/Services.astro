---
// Services Section - Dark industrial premium design
---

<section id="servicios" class="svc-section">
  <!-- Ambient orbs -->
  <div class="svc-orb svc-orb-1"></div>
  <div class="svc-orb svc-orb-2"></div>

  <!-- Grid texture overlay -->
  <div class="svc-grid-overlay"></div>

  <!-- Logo watermark -->
  <div class="svc-watermark" aria-hidden="true">
    <img src="/logo/logo2.svg" alt="" />
  </div>

  <!-- Top golden edge -->
  <div class="svc-edge-top"></div>

  <div class="svc-container">

    <!-- SECTION HEADER -->
    <div class="svc-header animate-on-scroll">
      <div class="svc-eyebrow">
        <span class="svc-eyebrow-rule"></span>
        <span class="svc-eyebrow-text">Lo que hacemos</span>
        <span class="svc-eyebrow-rule svc-eyebrow-rule--r"></span>
      </div>
      <h2 class="svc-title">Nuestros <em>Servicios</em></h2>
      <div class="svc-scratch"></div>
      <p class="svc-subtitle">
        Soluciones integrales para el <strong>mantenimiento y reparacion</strong> de sopladores industriales.
        Servicio profesional <strong>en sitio</strong> para mantener su operacion sin interrupciones.
      </p>
    </div>

    <!-- IMAGE BAND -->
    <div class="svc-imgband animate-on-scroll">
      <!-- Left: texto / stats -->
      <div class="svc-imgband-text">
        <div class="svc-imgband-tag">
          <span class="svc-imgband-dot"></span>
          Trabajo en sitio
        </div>
        <h3 class="svc-imgband-heading">Nos desplazamos a su planta</h3>
        <p class="svc-imgband-body">Nuestros técnicos se trasladan directamente a sus instalaciones para minimizar los tiempos de parada y garantizar la continuidad operativa.</p>
        <div class="svc-imgband-stats">
          <div class="svc-imgband-stat">
            <span class="svc-imgband-stat-num">100<span class="svc-imgband-stat-unit">%</span></span>
            <span class="svc-imgband-stat-label">Servicio en sitio</span>
          </div>
          <div class="svc-imgband-divider"></div>
          <div class="svc-imgband-stat">
            <span class="svc-imgband-stat-num">+10<span class="svc-imgband-stat-unit">años</span></span>
            <span class="svc-imgband-stat-label">de experiencia</span>
          </div>
        </div>
      </div>
      <!-- Right: imagen portrait -->
      <div class="svc-imgband-imgwrap">
        <img src="/imagenes/S3AImagnes/repicky28.png" alt="Técnico en planta industrial" class="svc-imgband-img" />
        <div class="svc-imgband-overlay"></div>
      </div>
    </div>

    <!-- CARDS GRID -->
    <div class="svc-grid">

      <!-- Card 1: Reparacion -->
      <div class="svc-card animate-on-scroll">
        <div class="svc-card-bar"></div>
        <div class="svc-card-num">01</div>
        <div class="svc-card-icon-wrap">
          <svg width="32" height="32" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.8" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z" />
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.8" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
          </svg>
        </div>
        <h3 class="svc-card-title">Reparacion de Sopladores</h3>
        <p class="svc-card-desc">Diagnostico y reparacion integral de sopladores industriales de todas las marcas y modelos. Identificamos y solucionamos fallas mecanicas, electricas y de rendimiento.</p>
        <ul class="svc-list">
          <li>Diagnostico preciso de fallas</li>
          <li>Reparacion de componentes mecanicos</li>
          <li>Correccion de problemas electricos</li>
          <li>Pruebas de funcionamiento</li>
        </ul>
        <a href="/contacto" class="svc-link">Solicitar servicio <svg width="16" height="16" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M9 5l7 7-7 7"/></svg></a>
      </div>

      <!-- Card 2: Preventivo (destacada) -->
      <div class="svc-card svc-card--featured animate-on-scroll">
        <div class="svc-featured-badge">Mas Solicitado</div>
        <div class="svc-card-num svc-card-num--light">02</div>
        <div class="svc-card-icon-wrap svc-card-icon-wrap--light">
          <svg width="32" height="32" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.8" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-6 9l2 2 4-4" />
          </svg>
        </div>
        <h3 class="svc-card-title svc-card-title--light">Mantenimiento Preventivo</h3>
        <p class="svc-card-desc svc-card-desc--light">Planes de mantenimiento programado disenados para evitar paradas no planificadas. Inspecciones periodicas y ajustes preventivos para maximizar la vida util de sus equipos.</p>
        <ul class="svc-list svc-list--light">
          <li>Inspecciones periodicas programadas</li>
          <li>Lubricacion y ajustes tecnicos</li>
          <li>Reemplazo de componentes de desgaste</li>
          <li>Reportes detallados de estado</li>
        </ul>
        <a href="/contacto" class="svc-link svc-link--light">Solicitar servicio <svg width="16" height="16" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M9 5l7 7-7 7"/></svg></a>
      </div>

      <!-- Card 3: Correctivo -->
      <div class="svc-card animate-on-scroll">
        <div class="svc-card-bar"></div>
        <div class="svc-card-num">03</div>
        <div class="svc-card-icon-wrap">
          <svg width="32" height="32" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.8" d="M13 10V3L4 14h7v7l9-11h-7z" />
          </svg>
        </div>
        <h3 class="svc-card-title">Mantenimiento Correctivo</h3>
        <p class="svc-card-desc">Intervencion rapida ante fallas inesperadas para minimizar tiempos de inactividad. Servicio de emergencia disponible las 24 horas para restablecer su operacion.</p>
        <ul class="svc-list">
          <li>Respuesta rapida ante emergencias</li>
          <li>Tecnicos especializados en sitio</li>
          <li>Solucion inmediata de fallas</li>
          <li>Minimizacion de tiempos muertos</li>
        </ul>
        <a href="/contacto" class="svc-link">Solicitar servicio <svg width="16" height="16" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M9 5l7 7-7 7"/></svg></a>
      </div>
    </div>

    <!-- BOTTOM CTA STRIP -->
    <div class="svc-cta animate-on-scroll">
      <div class="svc-cta-content">
        <div class="svc-cta-icon">
          <svg width="28" height="28" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z" />
          </svg>
        </div>
        <div>
          <h3 class="svc-cta-title">Necesita un servicio personalizado?</h3>
          <p class="svc-cta-text">Contactenos para evaluar sus necesidades y disenlar una solucion a medida para su operacion industrial.</p>
        </div>
      </div>
      <a href="/contacto" class="svc-cta-btn">Solicitar Cotizacion <svg width="18" height="18" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M13 7l5 5m0 0l-5 5m5-5H6"/></svg></a>
    </div>
  </div>

  <!-- Bottom golden edge -->
  <div class="svc-edge-bottom"></div>
</section>

<style>
.svc-section {
  position: relative;
  padding: 110px 0 120px;
  overflow: hidden;
  background: linear-gradient(170deg, #13202a 0%, #1a2d3a 40%, #1e3040 70%, #141f27 100%);
  color: #fff;
}
.svc-orb {
  position: absolute;
  border-radius: 50%;
  filter: blur(90px);
  pointer-events: none;
}
.svc-orb-1 {
  width: 520px; height: 520px;
  top: -120px; left: -100px;
  background: radial-gradient(circle, rgba(170,126,86,.14) 0%, transparent 70%);
}
.svc-orb-2 {
  width: 440px; height: 440px;
  bottom: -80px; right: -80px;
  background: radial-gradient(circle, rgba(123,84,51,.10) 0%, transparent 70%);
}
.svc-grid-overlay {
  position: absolute;
  inset: 0;
  background:
    repeating-linear-gradient(90deg, transparent 0px, transparent 79px, rgba(255,255,255,.014) 79px, rgba(255,255,255,.014) 80px),
    repeating-linear-gradient(0deg, transparent 0px, transparent 79px, rgba(255,255,255,.014) 79px, rgba(255,255,255,.014) 80px);
  pointer-events: none;
}
.svc-watermark {
  position: absolute;
  top: 50%; right: -80px;
  transform: translateY(-50%);
  pointer-events: none;
  opacity: .04;
}
.svc-watermark img { width: 500px; height: 500px; object-fit: contain; }
@media(max-width:600px){ .svc-watermark { display: none; } }
.svc-edge-top, .svc-edge-bottom {
  position: absolute;
  left: 0; right: 0;
  height: 3px;
  background: linear-gradient(90deg, transparent 0%, rgba(170,126,86,.5) 20%, #cca373 50%, rgba(170,126,86,.5) 80%, transparent 100%);
  pointer-events: none;
}
.svc-edge-top { top: 0; }
.svc-edge-bottom { bottom: 0; opacity: .4; }
.svc-container {
  max-width: 1240px;
  margin: 0 auto;
  padding: 0 32px;
  position: relative;
  z-index: 2;
}
@media(max-width:640px){ .svc-container { padding: 0 20px; } }
.svc-header {
  text-align: center;
  max-width: 720px;
  margin: 0 auto 72px;
}
.svc-eyebrow {
  display: inline-flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 26px;
}
.svc-eyebrow-rule {
  width: 36px; height: 2px;
  border-radius: 2px;
  background: linear-gradient(90deg, transparent, #cca373);
}
.svc-eyebrow-rule--r {
  background: linear-gradient(90deg, #cca373, transparent);
}
.svc-eyebrow-text {
  font-family: var(--font-heading);
  font-size: .75rem;
  font-weight: 700;
  letter-spacing: .18em;
  text-transform: uppercase;
  color: #cca373;
}
.svc-title {
  font-family: var(--font-display);
  font-size: clamp(2.8rem, 5vw, 4.5rem);
  font-weight: 400;
  line-height: 1.0;
  color: #fff;
  letter-spacing: 0.02em;
  text-transform: uppercase;
  margin-bottom: 24px;
}
.svc-title em {
  font-style: italic;
  background: linear-gradient(135deg, #aa7e56, #cca373, #aa7e56);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}
.svc-scratch {
  width: 60px; height: 4px;
  background: linear-gradient(90deg, #7b5433, #aa7e56, #cca373);
  border-radius: 2px;
  margin: 0 auto 22px;
}
.svc-subtitle {
  font-family: var(--font-body);
  font-size: clamp(.95rem, 1.5vw, 1.1rem);
  line-height: 1.75;
  color: rgba(255,255,255,.62);
}
.svc-subtitle strong { color: rgba(255,255,255,.88); font-weight: 600; }
.svc-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 24px;
  margin-bottom: 64px;
}
@media(max-width:960px) { .svc-grid { grid-template-columns: 1fr; gap: 20px; } }
@media(min-width:601px) and (max-width:960px) { .svc-grid { grid-template-columns: repeat(2, 1fr); } }
@media(max-width:600px) { .svc-grid { grid-template-columns: 1fr; } }
.svc-card {
  position: relative;
  background: rgba(255,255,255,.04);
  border: 1px solid rgba(255,255,255,.09);
  border-radius: 20px;
  padding: 36px 30px 32px;
  display: flex;
  flex-direction: column;
  overflow: hidden;
  transition: transform .4s cubic-bezier(.22,1,.36,1), box-shadow .4s ease, border-color .4s ease, background .4s ease;
}
.svc-card:hover {
  transform: translateY(-8px);
  background: rgba(255,255,255,.07);
  border-color: rgba(170,126,86,.35);
  box-shadow: 0 24px 60px rgba(0,0,0,.35), 0 0 0 1px rgba(170,126,86,.15);
}
@media(max-width:640px) { .svc-card { padding: 28px 22px 26px; } }
.svc-card-bar {
  position: absolute;
  left: 0; top: 0; bottom: 0;
  width: 3px;
  background: linear-gradient(180deg, #7b5433, #aa7e56, #cca373);
  border-radius: 3px 0 0 3px;
  transform: scaleY(0);
  transform-origin: top;
  transition: transform .4s cubic-bezier(.22,1,.36,1);
}
.svc-card:hover .svc-card-bar { transform: scaleY(1); }
.svc-card--featured {
  background: linear-gradient(145deg, #aa7e56 0%, #8b6644 40%, #7b5433 100%);
  border-color: rgba(204,163,115,.4);
  box-shadow: 0 16px 48px rgba(123,84,51,.4), 0 4px 16px rgba(0,0,0,.3);
}
.svc-card--featured:hover {
  transform: translateY(-8px);
  background: linear-gradient(145deg, #b8895e 0%, #9a7350 40%, #8a6040 100%);
  box-shadow: 0 28px 64px rgba(123,84,51,.5);
  border-color: rgba(204,163,115,.6);
}
.svc-featured-badge {
  position: absolute;
  top: 20px; right: 20px;
  padding: 5px 14px;
  background: rgba(255,255,255,.2);
  border: 1px solid rgba(255,255,255,.35);
  border-radius: 100px;
  font-family: var(--font-heading);
  font-size: .68rem;
  font-weight: 700;
  letter-spacing: .12em;
  text-transform: uppercase;
  color: #fff;
  backdrop-filter: blur(6px);
}
.svc-card-num {
  font-family: var(--font-display);
  font-size: 3.5rem;
  font-weight: 400;
  color: rgba(170,126,86,.2);
  line-height: 1;
  margin-bottom: 16px;
  letter-spacing: 0.02em;
  transition: color .4s ease;
}
.svc-card:hover .svc-card-num { color: rgba(170,126,86,.4); }
.svc-card-num--light { color: rgba(255,255,255,.2); }
@media(max-width:640px) { .svc-card-num { font-size: 2.8rem; } }
.svc-card-icon-wrap {
  width: 56px; height: 56px;
  border-radius: 14px;
  background: rgba(170,126,86,.15);
  border: 1px solid rgba(170,126,86,.25);
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 20px;
  color: #cca373;
  transition: background .4s ease, transform .4s cubic-bezier(.22,1,.36,1);
}
.svc-card:hover .svc-card-icon-wrap {
  background: rgba(170,126,86,.22);
  transform: scale(1.08) rotate(3deg);
}
.svc-card-icon-wrap--light {
  background: rgba(255,255,255,.15);
  border-color: rgba(255,255,255,.25);
  color: #fff;
}
.svc-card-title {
  font-family: var(--font-heading);
  font-size: clamp(1.1rem, 2vw, 1.35rem);
  font-weight: 700;
  color: #fff;
  text-transform: uppercase;
  letter-spacing: .04em;
  line-height: 1.3;
  margin-bottom: 14px;
}
.svc-card-desc {
  font-family: var(--font-body);
  font-size: .95rem;
  line-height: 1.7;
  color: rgba(255,255,255,.58);
  margin-bottom: 22px;
  flex-grow: 1;
}
.svc-card-desc--light { color: rgba(255,255,255,.82); }
.svc-list {
  list-style: none;
  padding: 0;
  margin: 0 0 26px;
  display: flex;
  flex-direction: column;
  gap: 8px;
}
.svc-list li {
  display: flex;
  align-items: center;
  gap: 10px;
  font-family: var(--font-body);
  font-size: .88rem;
  color: rgba(255,255,255,.65);
}
.svc-list li::before {
  content: '';
  width: 5px; height: 5px;
  border-radius: 50%;
  background: #aa7e56;
  flex-shrink: 0;
}
.svc-list--light li { color: rgba(255,255,255,.85); }
.svc-list--light li::before { background: rgba(255,255,255,.7); }
.svc-link {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  font-family: var(--font-heading);
  font-size: .82rem;
  font-weight: 700;
  letter-spacing: .1em;
  text-transform: uppercase;
  color: #aa7e56;
  text-decoration: none;
  margin-top: auto;
  padding-top: 4px;
  transition: gap .3s ease, color .3s ease;
}
.svc-link:hover { gap: 14px; color: #cca373; }
.svc-link--light { color: rgba(255,255,255,.85); }
.svc-link--light:hover { color: #fff; gap: 14px; }
.svc-cta {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 24px;
  padding: 32px 40px;
  background: rgba(255,255,255,.05);
  border: 1px solid rgba(170,126,86,.2);
  border-radius: 20px;
  backdrop-filter: blur(8px);
  flex-wrap: wrap;
}
@media(max-width:640px) {
  .svc-cta { padding: 24px 22px; flex-direction: column; align-items: flex-start; }
}
.svc-cta-content {
  display: flex;
  align-items: center;
  gap: 18px;
}
@media(max-width:480px) { .svc-cta-content { flex-direction: column; align-items: flex-start; gap: 12px; } }
.svc-cta-icon {
  width: 52px; height: 52px;
  border-radius: 14px;
  background: rgba(170,126,86,.15);
  border: 1px solid rgba(170,126,86,.25);
  display: flex;
  align-items: center;
  justify-content: center;
  color: #cca373;
  flex-shrink: 0;
}
.svc-cta-title {
  font-family: var(--font-heading);
  font-size: clamp(1rem, 2vw, 1.2rem);
  font-weight: 700;
  color: #fff;
  text-transform: uppercase;
  letter-spacing: .04em;
  margin-bottom: 4px;
}
.svc-cta-text {
  font-family: var(--font-body);
  font-size: .9rem;
  color: rgba(255,255,255,.55);
  line-height: 1.5;
}
.svc-cta-btn {
  display: inline-flex;
  align-items: center;
  gap: 10px;
  padding: 14px 28px;
  background: linear-gradient(135deg, #aa7e56 0%, #8b6644 100%);
  color: #fff;
  font-family: var(--font-heading);
  font-size: .85rem;
  font-weight: 700;
  letter-spacing: .1em;
  text-transform: uppercase;
  text-decoration: none;
  border-radius: 12px;
  white-space: nowrap;
  transition: transform .3s cubic-bezier(.22,1,.36,1), box-shadow .3s ease;
  box-shadow: 0 6px 24px rgba(123,84,51,.35);
  flex-shrink: 0;
}
.svc-cta-btn:hover {
  transform: translateY(-3px);
  box-shadow: 0 12px 32px rgba(123,84,51,.45);
}
@media(min-width:601px) and (max-width:960px) {
  .svc-card--featured { grid-column: 1 / -1; }
}

/* ── IMAGE BAND ── */
.svc-imgband {
  display: grid;
  grid-template-columns: 1fr 340px;
  gap: 0;
  border-radius: 20px;
  overflow: hidden;
  margin-bottom: 64px;
  border: 1px solid rgba(170,126,86,.2);
  box-shadow: 0 12px 40px rgba(0,0,0,.4);
  background: rgba(255,255,255,.04);
  min-height: 420px;
}
@media(max-width:860px) {
  .svc-imgband { grid-template-columns: 1fr; min-height: auto; }
}
@media(max-width:640px) { .svc-imgband { margin-bottom: 40px; } }

/* Left text panel */
.svc-imgband-text {
  padding: 48px 44px;
  display: flex;
  flex-direction: column;
  justify-content: center;
  gap: 20px;
}
@media(max-width:640px) { .svc-imgband-text { padding: 32px 24px; } }

.svc-imgband-tag {
  display: inline-flex;
  align-items: center;
  gap: 10px;
  font-family: var(--font-heading);
  font-size: .72rem;
  font-weight: 700;
  letter-spacing: .16em;
  text-transform: uppercase;
  color: #cca373;
  background: rgba(170,126,86,.1);
  border: 1px solid rgba(170,126,86,.25);
  padding: 6px 16px;
  border-radius: 100px;
  width: fit-content;
}
.svc-imgband-dot {
  width: 6px; height: 6px;
  border-radius: 50%;
  background: #cca373;
  animation: pulse-dot 2.5s ease-in-out infinite;
  flex-shrink: 0;
}
@keyframes pulse-dot {
  0%, 100% { opacity: 1; transform: scale(1); }
  50%       { opacity: .4; transform: scale(.7); }
}
.svc-imgband-heading {
  font-family: var(--font-display);
  font-size: clamp(1.6rem, 3vw, 2.4rem);
  font-weight: 400;
  color: #fff;
  text-transform: uppercase;
  letter-spacing: .03em;
  line-height: 1.1;
  margin: 0;
}
.svc-imgband-body {
  font-family: var(--font-body);
  font-size: .95rem;
  line-height: 1.75;
  color: rgba(255,255,255,.6);
  max-width: 440px;
  margin: 0;
}
.svc-imgband-stats {
  display: flex;
  align-items: center;
  gap: 24px;
  margin-top: 8px;
}
.svc-imgband-stat {
  display: flex;
  flex-direction: column;
  gap: 4px;
}
.svc-imgband-stat-num {
  font-family: var(--font-display);
  font-size: 2.2rem;
  font-weight: 400;
  color: #cca373;
  line-height: 1;
  letter-spacing: .02em;
}
.svc-imgband-stat-unit {
  font-size: 1rem;
  margin-left: 2px;
}
.svc-imgband-stat-label {
  font-family: var(--font-heading);
  font-size: .7rem;
  font-weight: 700;
  letter-spacing: .12em;
  text-transform: uppercase;
  color: rgba(255,255,255,.45);
}
.svc-imgband-divider {
  width: 1px;
  height: 48px;
  background: rgba(170,126,86,.25);
}

/* Right image panel */
.svc-imgband-imgwrap {
  position: relative;
  overflow: hidden;
}
@media(max-width:860px) {
  .svc-imgband-imgwrap { height: 360px; }
}
.svc-imgband-img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: center center;
  display: block;
  transition: transform 6s ease;
}
.svc-imgband:hover .svc-imgband-img { transform: scale(1.04); }
.svc-imgband-overlay {
  position: absolute;
  inset: 0;
  background: linear-gradient(to right, rgba(20,31,39,.6) 0%, transparent 50%);
}
</style>
