<style>
  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

  :root {
    --gold:       #aa7e56;
    --gold-light: #d4a574;
    --gold-pale:  #cca373;
    --gold-dark:  #7b5433;
    --navy:       #1e313f;
    --navy-mid:   #2d4558;
    --navy-soft:  #334c60;
    --navy-muted: #5a7889;
    --cream:      #fdfcfb;
    --white:      #ffffff;
    --border-gold: rgba(170,126,86,.18);
    --border-navy: rgba(30,49,63,.15);
    --shadow-sm:  0 2px 12px rgba(0,0,0,.06);
    --shadow-md:  0 8px 32px rgba(0,0,0,.10);
    --shadow-lg:  0 20px 60px rgba(0,0,0,.12);
    --radius-card: 24px;
    --radius-btn:  16px;
    --ease-out: cubic-bezier(.22,1,.36,1);
  }

  html { font-size: 16px; scroll-behavior: smooth; }

  body {
    font-family: var(--font-body);
    background: var(--white);
    color: var(--navy);
    -webkit-font-smoothing: antialiased;
  }

  /* ─── SECTION ─────────────────────────────────────────── */
  #sectores {
    position: relative;
    padding: 120px 0 140px;
    overflow: hidden;
    background: linear-gradient(180deg,
      #fff 0%, #faf8f5 40%, #f8f6f2 70%, #fff 100%);
  }

  /* Ambient orbs */
  .orb {
    position: absolute;
    border-radius: 50%;
    filter: blur(80px);
    pointer-events: none;
    will-change: transform;
  }
  .orb-1 {
    width: 560px; height: 560px;
    top: -80px; left: -120px;
    background: radial-gradient(circle, rgba(170,126,86,.13) 0%, transparent 70%);
    animation: drift1 14s ease-in-out infinite;
  }
  .orb-2 {
    width: 480px; height: 480px;
    bottom: 0; right: -100px;
    background: radial-gradient(circle, rgba(30,49,63,.10) 0%, transparent 70%);
    animation: drift2 18s ease-in-out infinite;
  }
  .orb-3 {
    width: 360px; height: 360px;
    top: 40%; left: 50%;
    transform: translate(-50%, -50%);
    background: radial-gradient(circle, rgba(123,84,51,.08) 0%, transparent 70%);
    animation: drift1 22s ease-in-out infinite reverse;
  }
  @keyframes drift1 {
    0%,100% { transform: translate(0,0) scale(1); }
    50%      { transform: translate(30px,20px) scale(1.05); }
  }
  @keyframes drift2 {
    0%,100% { transform: translate(0,0); }
    50%      { transform: translate(-25px,15px); }
  }

  /* Subtle grain texture overlay */
  #sectores::before {
    content: '';
    position: absolute;
    inset: 0;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)' opacity='0.03'/%3E%3C/svg%3E");
    pointer-events: none;
    z-index: 0;
    opacity: .4;
  }

  .container {
    max-width: 1280px;
    margin: 0 auto;
    padding: 0 32px;
    position: relative;
    z-index: 2;
  }

  /* ─── HEADER ──────────────────────────────────────────── */
  .section-header {
    text-align: center;
    max-width: 780px;
    margin: 0 auto 80px;
  }

  .eyebrow-badge {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    padding: 10px 22px;
    border-radius: 100px;
    background: rgba(170,126,86,.08);
    border: 1px solid rgba(170,126,86,.25);
    margin-bottom: 32px;
    backdrop-filter: blur(8px);
  }
  .eyebrow-badge .dot {
    width: 7px; height: 7px;
    border-radius: 50%;
    background: var(--gold);
    animation: pulse-dot 2s ease-in-out infinite;
  }
  @keyframes pulse-dot {
    0%,100% { opacity: 1; transform: scale(1); }
    50%      { opacity: .5; transform: scale(.75); }
  }
  .eyebrow-badge span {
    font-size: .78rem;
    font-weight: 700;
    letter-spacing: .12em;
    text-transform: uppercase;
    color: var(--gold);
  }

  .section-headline {
    font-family: var(--font-display);
    font-size: clamp(3rem, 5.5vw, 5rem);
    font-weight: 400;
    line-height: 1.0;
    color: var(--navy);
    margin-bottom: 28px;
    letter-spacing: 0.02em;
    text-transform: uppercase;
  }
  .section-headline em {
    font-style: italic;
    background: linear-gradient(135deg, var(--gold) 0%, var(--gold-light) 50%, var(--gold-dark) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  /* animated underline */
  .underline-anim {
    position: relative;
    display: inline-block;
    padding-right: 0.12em;
  }
  .underline-anim::after {
    content: '';
    position: absolute;
    left: 0; bottom: -6px;
    height: 3px;
    width: 100%;
    border-radius: 2px;
    background: linear-gradient(90deg, var(--gold), var(--gold-light), var(--gold-dark));
    background-size: 200% 100%;
    animation: shimmer-line 3s linear infinite;
  }
  @keyframes shimmer-line {
    0%   { background-position: 200% 0; }
    100% { background-position: -200% 0; }
  }

  .section-subtext {
    font-size: clamp(.95rem, 1.5vw, 1.15rem);
    line-height: 1.75;
    color: var(--navy-muted);
    font-weight: 400;
  }
  .section-subtext strong { color: var(--navy); font-weight: 700; }

  /* ─── THIN SEPARATOR LINE ─────────────────────────────── */
  .divider-line {
    width: 60px; height: 3px;
    border-radius: 2px;
    background: linear-gradient(90deg, var(--gold), var(--gold-light));
    margin: 0 auto 28px;
  }

  /* ─── CARDS GRID ──────────────────────────────────────── */
  .industries-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 24px;
    margin-bottom: 100px;
  }
  @media (max-width: 1100px) {
    .industries-grid { grid-template-columns: repeat(2, 1fr); gap: 20px; }
  }
  @media (max-width: 600px) {
    /* ── Grid: unified panel with internal hairline dividers ── */
    .industries-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 0;
      margin-bottom: 44px;
      background: rgba(255,255,255,.80);
      border-radius: 22px;
      overflow: hidden;
      box-shadow: 0 2px 20px rgba(0,0,0,.07);
      border: 1px solid rgba(170,126,86,.13);
    }

    /* ── Cards: fully transparent, no box ── */
    .industry-card {
      padding: 26px 12px 22px;
      border-radius: 0;
      border: none;
      border-bottom: 1px solid rgba(170,126,86,.10);
      box-shadow: none;
      background: transparent;
      display: flex;
      flex-direction: column;
      align-items: center;
      text-align: center;
      cursor: default;
      transition: background .25s ease;
    }
    /* Tap highlight on touch */
    .industry-card:active { background: rgba(170,126,86,.06); }

    /* Right divider: left-column cards */
    .industry-card:nth-child(odd) {
      border-right: 1px solid rgba(170,126,86,.10);
    }
    /* Remove bottom border from last 2 cards */
    .industry-card:nth-last-child(-n+2) {
      border-bottom: none;
    }

    /* Kill desktop decorations */
    .industry-card::before { display: none; }
    .card-glow         { display: none; }
    .industry-card:hover {
      transform: none;
      box-shadow: none;
      border-color: transparent;
    }
    .industry-card:nth-child(odd):hover {
      border-right: 1px solid rgba(170,126,86,.10);
    }
    .industry-card:hover .card-icon-wrap { transform: none; }

    /* ── Icon ── */
    .card-icon-wrap {
      width: 56px; height: 56px;
      border-radius: 18px;
      margin-bottom: 13px;
      flex-shrink: 0;
    }
    .card-icon-wrap svg { width: 24px; height: 24px; }

    /* ── Text ── */
    .card-title {
      font-size: .88rem;
      font-weight: 800;
      margin-bottom: 5px;
      letter-spacing: 0.01em;
      line-height: 1.15;
      color: var(--navy);
    }
    .card-subtitle {
      font-size: .64rem;
      letter-spacing: 0;
      line-height: 1.4;
      margin-bottom: 0;
    }
  }

  /* ─── SINGLE CARD ─────────────────────────────────────── */
  .industry-card {
    position: relative;
    background: var(--white);
    border-radius: var(--radius-card);
    padding: 28px 24px 24px;
    border: 1px solid rgba(170,126,86,.14);
    box-shadow: var(--shadow-sm);
    transition:
      transform .5s var(--ease-out),
      box-shadow .5s var(--ease-out),
      border-color .4s ease;
    cursor: pointer;
    overflow: hidden;
  }

  /* Top accent bar */
  .industry-card::before {
    content: '';
    position: absolute;
    top: 0; left: 0; right: 0;
    height: 3px;
    background: var(--card-accent, linear-gradient(90deg, #aa7e56, #d4a574));
    transform: scaleX(0);
    transform-origin: left;
    transition: transform .5s var(--ease-out);
    border-radius: 24px 24px 0 0;
  }
  .industry-card:hover::before { transform: scaleX(1); }
  .industry-card:hover {
    transform: translateY(-10px);
    box-shadow: 0 24px 64px rgba(0,0,0,.13);
    border-color: rgba(170,126,86,.30);
  }

  /* Glow bloom on hover */
  .card-glow {
    position: absolute;
    inset: 0;
    border-radius: inherit;
    opacity: 0;
    transition: opacity .6s ease;
    pointer-events: none;
  }
  .industry-card:hover .card-glow { opacity: 1; }

  /* Number badge */
  .card-num { display: none; }

  /* Icon wrapper */
  .card-icon-wrap {
    width: 52px; height: 52px;
    border-radius: 14px;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 16px;
    transition: transform .5s var(--ease-out);
    position: relative;
    z-index: 1;
  }
  .industry-card:hover .card-icon-wrap {
    transform: scale(1.08) rotate(5deg);
  }
  .card-icon-wrap svg {
    width: 28px; height: 28px;
    stroke: #fff;
  }

  /* Card text */
  .card-title {
    font-family: var(--font-heading);
    font-size: 1.4rem;
    font-weight: 800;
    color: var(--navy);
    margin-bottom: 6px;
    letter-spacing: 0.02em;
    text-transform: uppercase;
  }
  .card-subtitle {
    font-size: .82rem;
    font-weight: 500;
    color: var(--gold);
    letter-spacing: .04em;
    text-transform: uppercase;
    margin-bottom: 20px;
  }
  .card-divider {
    height: 1px;
    background: rgba(170,126,86,.12);
    margin-bottom: 20px;
  }
  .card-features {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 10px;
  }
  .card-features li {
    display: flex;
    align-items: center;
    gap: 9px;
    font-size: .87rem;
    font-weight: 500;
    color: var(--navy-soft);
    line-height: 1.4;
  }
  .card-features li::before {
    content: '';
    width: 6px; height: 6px;
    border-radius: 50%;
    background: var(--gold);
    flex-shrink: 0;
  }

  /* Card hover CTA */
  .card-cta {
    display: flex;
    align-items: center;
    gap: 6px;
    margin-top: 24px;
    font-size: .83rem;
    font-weight: 700;
    color: var(--gold);
    letter-spacing: .04em;
    opacity: 0;
    transform: translateY(6px);
    transition: opacity .4s ease, transform .4s var(--ease-out);
  }
  .industry-card:hover .card-cta {
    opacity: 1;
    transform: translateY(0);
  }
  .card-cta svg {
    width: 14px; height: 14px;
    stroke: var(--gold);
    transition: transform .4s var(--ease-out);
  }
  .industry-card:hover .card-cta svg { transform: translateX(4px); }

  /* ─── CARD VARIANTS ───────────────────────────────────── */
  /* gold */
  .card-gold { --card-accent: linear-gradient(90deg, #aa7e56, #d4a574); }
  .card-gold .card-icon-wrap { background: linear-gradient(135deg, #aa7e56, #d4a574); box-shadow: 0 8px 24px rgba(170,126,86,.30); }
  .card-gold .card-glow { background: radial-gradient(ellipse at 30% 0%, rgba(170,126,86,.07) 0%, transparent 60%); }

  /* navy */
  .card-navy { --card-accent: linear-gradient(90deg, #1e313f, #334c60); }
  .card-navy .card-icon-wrap { background: linear-gradient(135deg, #1e313f, #334c60); box-shadow: 0 8px 24px rgba(30,49,63,.30); }
  .card-navy .card-glow { background: radial-gradient(ellipse at 30% 0%, rgba(30,49,63,.06) 0%, transparent 60%); }
  .card-navy .card-num { color: var(--navy-soft); }
  .card-navy .card-features li::before { background: var(--navy-soft); }
  .card-navy .card-cta { color: var(--navy-soft); }
  .card-navy .card-cta svg { stroke: var(--navy-soft); }

  /* rust */
  .card-rust { --card-accent: linear-gradient(90deg, #7b5433, #aa7e56); }
  .card-rust .card-icon-wrap { background: linear-gradient(135deg, #7b5433, #aa7e56); box-shadow: 0 8px 24px rgba(123,84,51,.30); }
  .card-rust .card-glow { background: radial-gradient(ellipse at 30% 0%, rgba(123,84,51,.07) 0%, transparent 60%); }
  .card-rust .card-num { color: #7b5433; }
  .card-rust .card-subtitle { color: #7b5433; }
  .card-rust .card-features li::before { background: #7b5433; }
  .card-rust .card-cta { color: #7b5433; }
  .card-rust .card-cta svg { stroke: #7b5433; }

  /* dark */
  .card-dark { --card-accent: linear-gradient(90deg, #223546, #334c60); }
  .card-dark .card-icon-wrap { background: linear-gradient(135deg, #223546, #334c60); box-shadow: 0 8px 24px rgba(34,53,70,.30); }
  .card-dark .card-glow { background: radial-gradient(ellipse at 30% 0%, rgba(34,53,70,.06) 0%, transparent 60%); }
  .card-dark .card-num { color: var(--navy-mid); }
  .card-dark .card-subtitle { color: var(--navy-mid); }
  .card-dark .card-features li::before { background: var(--navy-mid); }
  .card-dark .card-cta { color: var(--navy-mid); }
  .card-dark .card-cta svg { stroke: var(--navy-mid); }

  /* warm */
  .card-warm { --card-accent: linear-gradient(90deg, #cca373, #aa7e56); }
  .card-warm .card-icon-wrap { background: linear-gradient(135deg, #cca373, #aa7e56); box-shadow: 0 8px 24px rgba(204,163,115,.30); }
  .card-warm .card-glow { background: radial-gradient(ellipse at 30% 0%, rgba(204,163,115,.07) 0%, transparent 60%); }
  .card-warm .card-num { color: #cca373; }
  .card-warm .card-subtitle { color: #cca373; }
  .card-warm .card-features li::before { background: #cca373; }
  .card-warm .card-cta { color: #cca373; }
  .card-warm .card-cta svg { stroke: #cca373; }

  /* dual */
  .card-dual { --card-accent: linear-gradient(90deg, #1e313f, #aa7e56); }
  .card-dual .card-icon-wrap { background: linear-gradient(135deg, #1e313f, #aa7e56); box-shadow: 0 8px 24px rgba(30,49,63,.28); }
  .card-dual .card-glow { background: radial-gradient(ellipse at 30% 0%, rgba(170,126,86,.06) 0%, transparent 60%); }

  /* ─── CTA BLOCK ───────────────────────────────────────── */
  .cta-block {
    position: relative;
    border-radius: 32px;
    overflow: hidden;
    padding: 80px 64px;
  }
  @media (max-width: 900px) { .cta-block { padding: 56px 36px; } }
  @media (max-width: 600px) { .cta-block { padding: 48px 24px; border-radius: 24px; } }

  .cta-bg {
    position: absolute;
    inset: 0;
    background: linear-gradient(145deg, #1e313f 0%, #2d4558 50%, #1a2c3a 100%);
    z-index: 0;
  }
  /* Gold mesh top-left */
  .cta-bg::before {
    content: '';
    position: absolute;
    top: -80px; left: -80px;
    width: 420px; height: 420px;
    border-radius: 50%;
    background: radial-gradient(circle, rgba(170,126,86,.22) 0%, transparent 65%);
    filter: blur(40px);
  }
  /* Gold mesh bottom-right */
  .cta-bg::after {
    content: '';
    position: absolute;
    bottom: -60px; right: -60px;
    width: 380px; height: 380px;
    border-radius: 50%;
    background: radial-gradient(circle, rgba(212,165,116,.18) 0%, transparent 65%);
    filter: blur(40px);
  }

  /* thin top gold line */
  .cta-line {
    position: absolute;
    top: 0; left: 0; right: 0;
    height: 2px;
    background: linear-gradient(90deg,
      transparent 0%, var(--gold) 30%, var(--gold-light) 50%, var(--gold) 70%, transparent 100%);
    z-index: 1;
  }

  .cta-inner {
    position: relative;
    z-index: 2;
    display: grid;
    grid-template-columns: 1fr 1fr;
    align-items: center;
    gap: 56px;
  }
  @media (max-width: 900px) {
    .cta-inner { grid-template-columns: 1fr; gap: 40px; text-align: center; }
  }

  /* Stats row inside CTA */
  .cta-stats {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    margin: 36px 0 0;
    border: 1px solid rgba(255,255,255,.10);
    border-radius: 14px;
    overflow: hidden;
    background: rgba(255,255,255,.04);
  }

  .stat-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    text-align: center;
    padding: 20px 12px;
    border-right: 1px solid rgba(255,255,255,.10);
  }
  .stat-item:last-child { border-right: none; }

  .stat-num {
    font-family: var(--font-display);
    font-size: 2.2rem;
    font-weight: 400;
    color: var(--gold-light);
    line-height: 1;
    margin-bottom: 6px;
    letter-spacing: 0.02em;
  }
  .stat-label {
    font-size: .72rem;
    font-weight: 600;
    letter-spacing: .06em;
    text-transform: uppercase;
    color: rgba(255,255,255,.45);
    line-height: 1.3;
  }

  .cta-eyebrow {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    font-size: .76rem;
    font-weight: 700;
    letter-spacing: .14em;
    text-transform: uppercase;
    color: var(--gold-light);
    margin-bottom: 18px;
  }
  .cta-eyebrow::before {
    content: '';
    display: block;
    width: 28px; height: 1px;
    background: var(--gold-light);
  }
  @media (max-width: 900px) {
    .cta-eyebrow { justify-content: center; }
  }

  .cta-headline {
    font-family: var(--font-display);
    font-size: clamp(2rem, 3.5vw, 3.2rem);
    font-weight: 400;
    color: var(--white);
    line-height: 1.15;
    letter-spacing: 0.02em;
    text-transform: uppercase;
  }
  .cta-headline em {
    font-style: italic;
    color: var(--gold-light);
  }

  .cta-body {
    font-size: 1rem;
    line-height: 1.7;
    color: rgba(255,255,255,.62);
    margin-top: 20px;
  }
  .cta-body strong { color: rgba(255,255,255,.88); font-weight: 600; }

  /* ── Right card ── */
  .cta-card {
    background: rgba(255,255,255,.06);
    border: 1px solid rgba(255,255,255,.12);
    border-radius: 20px;
    padding: 32px 36px;
    backdrop-filter: blur(8px);
    display: flex;
    flex-direction: column;
    gap: 24px;
  }
  @media (max-width: 600px) { .cta-card { padding: 24px 20px; } }

  .cta-card-title {
    font-family: var(--font-heading);
    font-size: .8rem;
    font-weight: 700;
    letter-spacing: .14em;
    text-transform: uppercase;
    color: var(--gold-light);
    padding-bottom: 16px;
    border-bottom: 1px solid rgba(255,255,255,.08);
  }

  .cta-checklist {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 14px;
  }
  .cta-checklist li {
    display: flex;
    align-items: center;
    gap: 12px;
    font-size: .95rem;
    font-weight: 500;
    color: rgba(255,255,255,.82);
    line-height: 1.4;
    text-align: left;
  }
  .cta-checklist li svg {
    width: 18px; height: 18px;
    stroke: var(--gold-light);
    flex-shrink: 0;
    background: rgba(170,126,86,.15);
    border-radius: 50%;
    padding: 2px;
  }

  /* Buttons column */
  .cta-actions {
    display: flex;
    flex-direction: column;
    gap: 12px;
  }
  @media (max-width: 900px) {
    .cta-actions { align-items: center; flex-direction: row; justify-content: center; flex-wrap: wrap; }
  }

  .btn {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    padding: 16px 30px;
    border-radius: var(--radius-btn);
    font-family: var(--font-heading);
    font-weight: 700;
    font-size: .95rem;
    letter-spacing: 0.05em;
    text-transform: uppercase;
    text-decoration: none;
    transition: transform .4s var(--ease-out), box-shadow .4s ease;
    white-space: nowrap;
    border: none;
    cursor: pointer;
  }
  .btn:hover { transform: translateY(-3px); }

  .btn-primary {
    background: linear-gradient(135deg, #aa7e56 0%, #7b5433 100%);
    color: #fff;
    box-shadow: 0 10px 32px rgba(170,126,86,.40);
    width: 100%;
    justify-content: center;
  }
  .btn-primary:hover { box-shadow: 0 16px 40px rgba(170,126,86,.50); }

  .btn-ghost {
    background: rgba(255,255,255,.07);
    border: 1px solid rgba(255,255,255,.18);
    color: rgba(255,255,255,.82);
    width: 100%;
    justify-content: center;
    backdrop-filter: blur(4px);
  }
  .btn-ghost:hover {
    background: rgba(255,255,255,.12);
    box-shadow: 0 8px 24px rgba(0,0,0,.15);
  }

  .btn svg { width: 16px; height: 16px; flex-shrink: 0; }
  .btn-arrow {
    width: 14px; height: 14px;
    margin-left: 4px;
    transition: transform .4s var(--ease-out);
  }
  .btn:hover .btn-arrow { transform: translateX(4px); }

  /* ─── ENTRY ANIMATIONS ────────────────────────────────── */
  .reveal {
    opacity: 0;
    transform: translateY(32px);
    transition: opacity .7s ease, transform .7s var(--ease-out);
  }
  .reveal.visible {
    opacity: 1;
    transform: translateY(0);
  }
  .reveal-delay-1 { transition-delay: .10s; }
  .reveal-delay-2 { transition-delay: .18s; }
  .reveal-delay-3 { transition-delay: .26s; }
  .reveal-delay-4 { transition-delay: .34s; }
  .reveal-delay-5 { transition-delay: .42s; }
  .reveal-delay-6 { transition-delay: .50s; }
  .reveal-delay-7 { transition-delay: .58s; }
  .reveal-delay-8 { transition-delay: .66s; }

  /* ─── RESPONSIVE TWEAKS ───────────────────────────────── */
  @media (max-width: 1280px) {
    .container { padding: 0 24px; }
  }
  @media (max-width: 768px) {
    #sectores { padding: 72px 0 88px; }
    .section-header { margin-bottom: 40px; }
    .industries-grid { margin-bottom: 44px; }
    .cta-block { padding: 48px 24px; }
    .stat-num { font-size: 1.8rem; }
    .section-headline { font-size: clamp(2rem, 8vw, 3.4rem); }
    .section-subtext { font-size: .9rem; }
  }
  @media (max-width: 480px) {
    .container { padding: 0 14px; }
    .cta-headline { font-size: 1.5rem; }
    #sectores { padding: 60px 0 72px; }
    .section-header { margin-bottom: 32px; }
    .eyebrow-badge { padding: 8px 16px; margin-bottom: 20px; }
    .eyebrow-badge span { font-size: .72rem; }
    .divider-line { margin-bottom: 20px; }
    .section-subtext { font-size: .85rem; line-height: 1.65; }
    .stat-num { font-size: 1.65rem; }
    .stat-label { font-size: .65rem; letter-spacing: .04em; }
    .stat-item { padding: 16px 8px; }
    .cta-stats { margin: 24px 0 0; border-radius: 12px; }
  }
</style>

<section id="sectores">

  <!-- Ambient orbs -->
  <div class="orb orb-1" aria-hidden="true"></div>
  <div class="orb orb-2" aria-hidden="true"></div>
  <div class="orb orb-3" aria-hidden="true"></div>

  <div class="container">

    <!-- ─── SECTION HEADER ─────────────────── -->
    <header class="section-header reveal">

      <div class="eyebrow-badge">
        <div class="dot"></div>
        <span>Sectores Industriales</span>
      </div>

      <div class="divider-line"></div>

      <h2 class="section-headline">
        Experiencia en<br>
        <em class="underline-anim">Múltiples Industrias</em>
      </h2>

      <p class="section-subtext" style="margin-top: 28px;">
        Amplia <strong>experiencia</strong> brindando soluciones especializadas en
        <strong>mantenimiento de sopladores industriales</strong>
        para los sectores productivos más exigentes del mercado.
      </p>
    </header>

    <!-- ─── CARDS GRID ─────────────────────── -->
    <div class="industries-grid" role="list">

      <!-- 01 Alimentaria -->
      <article class="industry-card card-gold reveal reveal-delay-1" role="listitem" tabindex="0">
        <div class="card-glow"></div>
        <span class="card-num">01</span>
        <div class="card-icon-wrap">
          <svg fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="1.8" aria-hidden="true">
            <!-- fork -->
            <path stroke-linecap="round" stroke-linejoin="round" d="M7 2v5m2-5v5m2-5v5a2 2 0 01-2 2H9a2 2 0 01-2-2M9 9v13"/>
            <!-- knife -->
            <path stroke-linecap="round" stroke-linejoin="round" d="M15.5 2C14 3.5 14 6 14.5 8a1.5 1.5 0 001.5 1v13"/>
          </svg>
        </div>
        <div class="card-num">01</div>
        <h3 class="card-title">Alimentaria</h3>
        <p class="card-subtitle">Panadería · Lácteos · Alimentos</p>
      </article>

      <!-- 02 Química -->
      <article class="industry-card card-navy reveal reveal-delay-2" role="listitem" tabindex="0">
        <div class="card-glow"></div>
        <div class="card-icon-wrap">
          <svg fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="1.8" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" d="M19.428 15.428a2 2 0 00-1.022-.547l-2.387-.477a6 6 0 00-3.86.517l-.318.158a6 6 0 01-3.86.517L6.05 15.21a2 2 0 00-1.806.547M8 4h8l-1 1v5.172a2 2 0 00.586 1.414l5 5c1.26 1.26.367 3.414-1.415 3.414H4.828c-1.782 0-2.674-2.154-1.414-3.414l5-5A2 2 0 009 10.172V5L8 4z"/>
          </svg>
        </div>
        <span class="card-num">02</span>
        <h3 class="card-title">Química</h3>
        <p class="card-subtitle">Petroquímica · Farmacéutica</p>
      </article>

      <!-- 03 Textil -->
      <article class="industry-card card-rust reveal reveal-delay-3" role="listitem" tabindex="0">
        <div class="card-glow"></div>
        <div class="card-icon-wrap">
          <svg fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="1.8" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" d="M14.121 14.121L19 19m-7-7l7-7m-7 7l-2.879 2.879M12 12L9.121 9.121m0 5.758a3 3 0 10-4.243 4.243 3 3 0 004.243-4.243zm0-5.758a3 3 0 10-4.243-4.243 3 3 0 004.243 4.243z"/>
          </svg>
        </div>
        <span class="card-num">03</span>
        <h3 class="card-title">Textil</h3>
        <p class="card-subtitle">Hilaterías · Teñido · Acabados</p>
      </article>

      <!-- 04 Minería -->
      <article class="industry-card card-dark reveal reveal-delay-4" role="listitem" tabindex="0">
        <div class="card-glow"></div>
        <div class="card-icon-wrap">
          <svg fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="1.8" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" d="M3 21v-4m0 0V5a2 2 0 012-2h6.5l1 1H21l-3 6 3 6h-8.5l-1-1H5a2 2 0 00-2 2zm9-13.5V9"/>
          </svg>
        </div>
        <span class="card-num">04</span>
        <h3 class="card-title">Minería</h3>
        <p class="card-subtitle">Extractiva · Minerales</p>
      </article>

      <!-- 05 Metalúrgica -->
      <article class="industry-card card-navy reveal reveal-delay-5" role="listitem" tabindex="0">
        <div class="card-glow"></div>
        <div class="card-icon-wrap">
          <svg fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="1.8" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" d="M17.657 18.657A8 8 0 016.343 7.343S7 9 9 10c0-2 .5-5 2.986-7C14 5 16.09 5.777 17.656 7.343A7.975 7.975 0 0120 13a7.975 7.975 0 01-2.343 5.657z"/>
            <path stroke-linecap="round" stroke-linejoin="round" d="M9.879 16.121A3 3 0 1012.015 11L11 14H9c0 .768.293 1.536.879 2.121z"/>
          </svg>
        </div>
        <span class="card-num">05</span>
        <h3 class="card-title">Metalúrgica</h3>
        <p class="card-subtitle">Fundición · Siderurgia · Forjas</p>
      </article>

      <!-- 06 Papel -->
      <article class="industry-card card-warm reveal reveal-delay-6" role="listitem" tabindex="0">
        <div class="card-glow"></div>
        <div class="card-icon-wrap">
          <svg fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="1.8" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"/>
          </svg>
        </div>
        <span class="card-num">06</span>
        <h3 class="card-title">Papel</h3>
        <p class="card-subtitle">Celulosa · Cartón Corrugado</p>
      </article>



    </div><!-- /industries-grid -->

    <!-- ─── BOTTOM CTA BLOCK ───────────────── -->
    <div class="cta-block reveal" style="transition-delay:.2s">
      <div class="cta-bg"></div>
      <div class="cta-line"></div>

      <div class="cta-inner">

        <!-- Left / Top: text + stats -->
        <div class="cta-left">

          <p class="cta-eyebrow">Soluciones a medida para su empresa</p>

          <h3 class="cta-headline">
            Trabajamos con<br>
            <em>cualquier industria</em>
          </h3>

          <p class="cta-body">
            Desde el diagnóstico hasta la puesta en marcha, S3A Industrial
            acompaña cada etapa del mantenimiento de sus sopladores.
            <strong>Presupuesto detallado, respuesta ágil y trabajo garantizado.</strong>
          </p>

          <!-- Stats row -->
          <div class="cta-stats" role="list">
            <div class="stat-item" role="listitem">
              <span class="stat-num">+100</span>
              <span class="stat-label">Clientes activos</span>
            </div>
            <div class="stat-item" role="listitem">
              <span class="stat-num">+20</span>
              <span class="stat-label">Sectores atendidos</span>
            </div>
            <div class="stat-item" role="listitem">
              <span class="stat-num">100%</span>
              <span class="stat-label">Servicio en sitio</span>
            </div>
          </div>

        </div>

        <!-- Right / Bottom: card con beneficios + botones -->
        <div class="cta-card">

          <p class="cta-card-title">¿Qué incluye nuestro servicio?</p>

          <ul class="cta-checklist">
            <li>
              <svg fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2.5"><path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7"/></svg>
              Diagnóstico técnico en sitio
            </li>
            <li>
              <svg fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2.5"><path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7"/></svg>
              Presupuesto detallado sin cargo
            </li>
            <li>
              <svg fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2.5"><path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7"/></svg>
              Mantenimiento preventivo y correctivo
            </li>
            <li>
              <svg fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2.5"><path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7"/></svg>
              Garantía sobre cada intervención
            </li>
          </ul>

          <div class="cta-actions">
            <a href="/contacto" class="btn btn-primary">
              Solicitar Presupuesto
              <svg class="btn-arrow" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2.5" aria-hidden="true">
                <path stroke-linecap="round" stroke-linejoin="round" d="M13 7l5 5m0 0l-5 5m5-5H6"/>
              </svg>
            </a>
            <a href="/servicios" class="btn btn-ghost">
              Ver todos los servicios
            </a>
          </div>

        </div>

      </div>
    </div><!-- /cta-block -->

  </div><!-- /container -->
</section>

<script>
  // Scroll-reveal observer
  const observer = new IntersectionObserver(
    (entries) => {
      entries.forEach(el => {
        if (el.isIntersecting) {
          el.target.classList.add('visible');
          observer.unobserve(el.target);
        }
      });
    },
    { threshold: 0.12 }
  );
  document.querySelectorAll('.reveal').forEach(el => observer.observe(el));
</script>
