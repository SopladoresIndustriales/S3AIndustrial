<style>
/* ═══════════════════════════════════════
   ROOT & RESET
═══════════════════════════════════════ */
:root {
  --primary:      #1e313f;
  --secondary:    #223546;
  --steel:        #334c60;
  --rust:         #7b5433;
  --golden:       #aa7e56;
  --golden-light: #cca373;
  --golden-dark:  #8b6644;
  --bg-cream:     #f4f1ec;
  --white:        #ffffff;

  --ease: cubic-bezier(.22,1,.36,1);
}

html { font-size: 16px; }
body {
  font-family: 'Barlow', sans-serif;
  background: #f4f1ec;
  -webkit-font-smoothing: antialiased;
}

/* ═══════════════════════════════════════
   FOOTER WRAPPER
═══════════════════════════════════════ */
.site-footer {
  position: relative;
  background: linear-gradient(170deg, #16262f 0%, #1a2d39 45%, #1c2f3c 100%);
  color: rgba(255,255,255,.62);
  overflow: hidden;
}

/* ─── background grid texture ─── */
.site-footer::before {
  content: '';
  position: absolute;
  inset: 0;
  background:
    repeating-linear-gradient(
      90deg,
      transparent 0px, transparent 79px,
      rgba(255,255,255,.018) 79px, rgba(255,255,255,.018) 80px
    ),
    repeating-linear-gradient(
      0deg,
      transparent 0px, transparent 79px,
      rgba(255,255,255,.018) 79px, rgba(255,255,255,.018) 80px
    );
  pointer-events: none;
  z-index: 0;
}

/* ─── warm glow ambiance ─── */
.site-footer::after {
  content: '';
  position: absolute;
  bottom: -100px; right: -100px;
  width: 560px; height: 560px;
  border-radius: 50%;
  background: radial-gradient(circle, rgba(170,126,86,.09) 0%, transparent 65%);
  filter: blur(60px);
  pointer-events: none;
  z-index: 0;
}

/* ─── animated top separator ─── */
.footer-top-bar {
  position: relative;
  z-index: 2;
  height: 3px;
  background: linear-gradient(
    90deg,
    transparent 0%,
    var(--rust) 15%,
    var(--golden) 35%,
    var(--golden-light) 50%,
    var(--golden) 65%,
    var(--rust) 85%,
    transparent 100%
  );
  background-size: 200% 100%;
  animation: shimmer-bar 6s linear infinite;
}
@keyframes shimmer-bar {
  0%   { background-position: 200% 0; }
  100% { background-position: -200% 0; }
}

/* ═══════════════════════════════════════
   MAIN FOOTER BODY
═══════════════════════════════════════ */
.footer-body {
  position: relative;
  z-index: 2;
  max-width: 1240px;
  margin: 0 auto;
  padding: 72px 36px 0;
}
@media(max-width:600px){ .footer-body{ padding: 56px 20px 0; } }

/* ─── top grid: brand + nav columns ─── */
.footer-grid {
  display: grid;
  grid-template-columns: 2fr 1fr 1fr;
  gap: 64px;
  padding-bottom: 60px;
  border-bottom: 1px solid rgba(255,255,255,.07);
}
@media(max-width:960px){
  .footer-grid { grid-template-columns: 1fr 1fr; gap: 48px; }
  .footer-brand { grid-column: 1 / -1; }
}
@media(max-width:580px){
  .footer-grid { grid-template-columns: 1fr; gap: 40px; }
}

/* ═══════════════════════════════════════
   BRAND COLUMN
═══════════════════════════════════════ */
.footer-brand {}

/* logo block */
.brand-logo-wrap {
  display: flex;
  align-items: center;
  gap: 18px;
  margin-bottom: 28px;
}

.brand-logo-img {
  /* Logo en colores originales – sin filtro */
  width: 120px;
  height: 120px;
  object-fit: contain;
  flex-shrink: 0;
  /* Subtle drop shadow to lift it off the dark bg */
  filter: drop-shadow(0 4px 16px rgba(0,0,0,.35));
  transition: transform .5s var(--ease), filter .4s ease;
}
.brand-logo-img:hover {
  transform: scale(1.05);
  filter: drop-shadow(0 6px 22px rgba(170,126,86,.35));
}

.brand-wordmark {}
.brand-name {
  font-family: var(--font-display);
  font-size: 2rem;
  font-weight: 400;
  color: var(--white);
  line-height: 1.1;
  letter-spacing: 0.02em;
  text-transform: uppercase;
}
.brand-tagline {
  font-family: var(--font-heading);
  font-size: .72rem;
  font-weight: 700;
  letter-spacing: .18em;
  text-transform: uppercase;
  color: var(--golden);
  margin-top: 4px;
}

/* divider scratch */
.brand-rule {
  width: 48px; height: 2px;
  background: linear-gradient(90deg, var(--golden-dark), var(--golden-light));
  border-radius: 2px;
  margin-bottom: 20px;
  position: relative;
}
.brand-rule::after {
  content:'';
  position:absolute;
  left:0; top:6px;
  width:28px; height:1px;
  background: rgba(170,126,86,.3);
  border-radius:1px;
}

.brand-desc {
  font-size: .9rem;
  line-height: 1.78;
  color: rgba(255,255,255,.50);
  max-width: 400px;
  margin-bottom: 32px;
}
.brand-desc strong { color: rgba(255,255,255,.75); font-weight: 600; }

/* ─── availability badge ─── */
.brand-avail {
  display: inline-flex;
  align-items: center;
  gap: 9px;
  padding: 9px 18px;
  border-radius: 100px;
  background: rgba(255,255,255,.05);
  border: 1px solid rgba(255,255,255,.10);
  margin-bottom: 32px;
}
.avail-pulse {
  width: 7px; height: 7px;
  border-radius: 50%;
  background: #4ade80;
  box-shadow: 0 0 0 0 rgba(74,222,128,.5);
  animation: pulse-green 2s ease-in-out infinite;
  flex-shrink: 0;
}
@keyframes pulse-green {
  0%   { box-shadow: 0 0 0 0 rgba(74,222,128,.5); }
  70%  { box-shadow: 0 0 0 7px rgba(74,222,128,0); }
  100% { box-shadow: 0 0 0 0 rgba(74,222,128,0); }
}
.avail-text {
  font-size: .78rem;
  font-weight: 600;
  color: rgba(255,255,255,.55);
  letter-spacing: .03em;
}
.avail-text strong { color: #4ade80; }

/* ─── social icons ─── */
.social-row {
  display: flex;
  gap: 10px;
}

.social-btn {
  width: 42px; height: 42px;
  border-radius: 12px;
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(255,255,255,.05);
  border: 1px solid rgba(255,255,255,.08);
  color: rgba(255,255,255,.45);
  text-decoration: none;
  transition: background .3s ease, color .3s ease, transform .4s var(--ease), box-shadow .3s ease, border-color .3s ease;
}
.social-btn svg { width: 17px; height: 17px; }
.social-btn:hover {
  background: rgba(170,126,86,.20);
  border-color: rgba(170,126,86,.40);
  color: var(--golden-light);
  transform: translateY(-4px);
  box-shadow: 0 8px 20px rgba(170,126,86,.20);
}

/* ═══════════════════════════════════════
   NAV COLUMNS
═══════════════════════════════════════ */
.footer-col-title {
  font-family: var(--font-heading);
  font-size: .72rem;
  font-weight: 700;
  letter-spacing: .18em;
  text-transform: uppercase;
  color: var(--golden);
  margin-bottom: 24px;
  display: flex;
  align-items: center;
  gap: 10px;
}
.footer-col-title::after {
  content: '';
  flex: 1;
  height: 1px;
  background: rgba(255,255,255,.07);
}

/* ─── links ─── */
.footer-links {
  list-style: none;
  display: flex;
  flex-direction: column;
  gap: 4px;
}

.footer-link {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  padding: 7px 0;
  font-size: .9rem;
  font-weight: 500;
  color: rgba(255,255,255,.48);
  text-decoration: none;
  transition: color .3s ease, gap .3s ease;
  position: relative;
}
.footer-link::before {
  content: '';
  width: 14px; height: 1px;
  background: var(--golden);
  opacity: 0;
  flex-shrink: 0;
  transition: opacity .3s ease, width .3s ease;
}
.footer-link:hover {
  color: rgba(255,255,255,.85);
  gap: 11px;
}
.footer-link:hover::before {
  opacity: 1;
}

/* ─── contact items ─── */
.contact-list {
  list-style: none;
  display: flex;
  flex-direction: column;
  gap: 16px;
}

.contact-item {
  display: flex;
  flex-direction: column;
  gap: 3px;
}
.contact-item-label {
  font-family: var(--font-heading);
  font-size: .68rem;
  font-weight: 700;
  letter-spacing: .14em;
  text-transform: uppercase;
  color: rgba(255,255,255,.28);
}
.contact-item-value {
  font-size: .9rem;
  font-weight: 500;
  color: rgba(255,255,255,.60);
  text-decoration: none;
  transition: color .3s ease;
  display: flex;
  align-items: center;
  gap: 7px;
}
.contact-item-value svg { width: 14px; height: 14px; stroke: var(--golden); flex-shrink: 0; }
a.contact-item-value:hover { color: var(--golden-light); }

/* ═══════════════════════════════════════
   BOTTOM BAR
═══════════════════════════════════════ */
.footer-bottom {
  position: relative;
  z-index: 2;
  max-width: 1240px;
  margin: 0 auto;
  padding: 0 36px;
}
@media(max-width:600px){ .footer-bottom{ padding: 0 20px; } }

.footer-bottom-inner {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 24px 0 36px;
  gap: 16px;
  flex-wrap: wrap;
}
@media(max-width:640px){ .footer-bottom-inner{ flex-direction:column; text-align:center; } }

.footer-copy {
  font-size: .8rem;
  color: rgba(255,255,255,.28);
  font-weight: 400;
  letter-spacing: .02em;
}
.footer-copy strong { color: var(--golden); font-weight: 700; }

.footer-legal {
  display: flex;
  gap: 24px;
  flex-wrap: wrap;
}
@media(max-width:640px){ .footer-legal{ justify-content:center; } }

.footer-legal a {
  font-size: .78rem;
  color: rgba(255,255,255,.24);
  text-decoration: none;
  transition: color .3s ease;
  font-weight: 500;
  letter-spacing: .02em;
}
.footer-legal a:hover { color: rgba(255,255,255,.55); }

/* ─── devcraft credit ─── */
.footer-devcraft {
  border-top: 1px solid rgba(255,255,255,.05);
  padding: 18px 0 28px;
  display: flex;
  justify-content: center;
}
.devcraft-link {
  display: inline-flex;
  align-items: center;
  gap: 9px;
  text-decoration: none;
  opacity: .45;
  transition: opacity .3s ease, transform .3s ease;
}
.devcraft-link:hover {
  opacity: .85;
  transform: translateY(-1px);
}
.devcraft-logo {
  width: 22px;
  height: 22px;
  object-fit: contain;
  border-radius: 4px;
}
.devcraft-text {
  font-size: .72rem;
  font-weight: 600;
  letter-spacing: .06em;
  text-transform: uppercase;
  color: rgba(255,255,255,.55);
}
.devcraft-text span {
  color: var(--golden-light);
  font-weight: 700;
}
</style>
</head>
<body>

<footer class="site-footer" role="contentinfo">

  <!-- animated gold separator -->
  <div class="footer-top-bar" aria-hidden="true"></div>

  <!-- MAIN GRID -->
  <div class="footer-body">
    <div class="footer-grid">

      <!-- ─── BRAND COLUMN ─── -->
      <div class="footer-brand">
        <div class="brand-logo-wrap">
          <!--
            IMPORTANTE: Quitar el filter:brightness(0)invert(1)
            El SVG se muestra con sus colores originales.
            Reemplazar src con la ruta real del logo.
          -->
          <img
            src="/logo/logo2.svg"
            alt="S3A Industrial"
            class="brand-logo-img"
          />
          <div class="brand-wordmark">
            <div class="brand-name">S3A Industrial</div>
            <div class="brand-tagline">Sopladores Industriales</div>
          </div>
        </div>

        <div class="brand-rule"></div>

        <p class="brand-desc">
          Especialistas en <strong>reparación y mantenimiento de sopladores industriales</strong>.
          Servicio técnico profesional para mantener la continuidad operativa
          de su empresa, con amplia trayectoria en el sector.
        </p>

        <!-- social -->
        <div class="social-row">
          <a href="#" class="social-btn" aria-label="LinkedIn">
            <svg fill="currentColor" viewBox="0 0 24 24">
              <path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433c-1.144 0-2.063-.926-2.063-2.065 0-1.138.92-2.063 2.063-2.063 1.14 0 2.064.925 2.064 2.063 0 1.139-.925 2.065-2.064 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/>
            </svg>
          </a>
          <a href="mailto:info@s3aindustrial.com" class="social-btn" aria-label="Email">
            <svg fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round"
                d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
            </svg>
          </a>
          <a href="https://wa.me/5491124599330" class="social-btn" aria-label="WhatsApp">
            <svg fill="currentColor" viewBox="0 0 24 24">
              <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z"/>
            </svg>
          </a>
        </div>
      </div>

      <!-- ─── LINKS COLUMN ─── -->
      <div>
        <h3 class="footer-col-title">Navegación</h3>
        <ul class="footer-links" role="list">
          <li><a href="/" class="footer-link">Inicio</a></li>
          <li><a href="/nosotros" class="footer-link">Nosotros</a></li>
          <li><a href="/servicios" class="footer-link">Servicios</a></li>
          <li><a href="/sectores" class="footer-link">Sectores</a></li>
          <li><a href="/cobertura" class="footer-link">Cobertura</a></li>
          <li><a href="/contacto" class="footer-link">Contacto</a></li>
        </ul>
      </div>

      <!-- ─── CONTACT COLUMN ─── -->
      <div>
        <h3 class="footer-col-title">Contacto</h3>
        <ul class="contact-list" role="list">
          <li class="contact-item">
            <span class="contact-item-label">Email</span>
            <a href="mailto:info@s3aindustrial.com" class="contact-item-value">
              <svg fill="none" viewBox="0 0 24 24" stroke-width="2">
                <path stroke-linecap="round" stroke-linejoin="round"
                  d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
              </svg>
              info@s3aindustrial.com
            </a>
          </li>
          <li class="contact-item">
            <span class="contact-item-label">Teléfono</span>
            <a href="https://wa.me/5491124599330" class="contact-item-value" target="_blank" rel="noopener noreferrer">
              <svg fill="none" viewBox="0 0 24 24" stroke-width="2">
                <path stroke-linecap="round" stroke-linejoin="round"
                  d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"/>
              </svg>
              +54 9 11 2459-9330
            </a>
          </li>
          <li class="contact-item">
            <span class="contact-item-label">Horario de atención</span>
            <span class="contact-item-value">
              <svg fill="none" viewBox="0 0 24 24" stroke-width="2">
                <path stroke-linecap="round" stroke-linejoin="round"
                  d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"/>
              </svg>
              Lun – Vie: 8:00 – 18:00
            </span>
          </li>
          <li class="contact-item">
            <span class="contact-item-label">Dirección</span>
            <span class="contact-item-value">
              <svg fill="none" viewBox="0 0 24 24" stroke-width="2">
                <path stroke-linecap="round" stroke-linejoin="round"
                  d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"/>
                <path stroke-linecap="round" stroke-linejoin="round"
                  d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"/>
              </svg>
              Hurlingham, Buenos Aires
            </span>
          </li>

        </ul>
      </div>

    </div><!-- /footer-grid -->
  </div><!-- /footer-body -->

  <!-- BOTTOM BAR -->
  <div class="footer-bottom">
    <div class="footer-bottom-inner">
      <p class="footer-copy">
        &copy; 2025 <strong>S3A Industrial</strong>. Todos los derechos reservados.
      </p>
      <nav class="footer-legal" aria-label="Legal">
        <a href="/privacidad">Política de Privacidad</a>
        <a href="/terminos">Términos de Uso</a>
        <a href="/sitemap">Mapa del Sitio</a>
      </nav>
    </div>

    <!-- DevCraft credit -->
    <div class="footer-devcraft">
      <a href="https://devcraftpablo.online/" class="devcraft-link" target="_blank" rel="noopener noreferrer" aria-label="Desarrollado por DevCraft">
        <img src="/logo/Dise%C3%B1o%20sin%20t%C3%ADtulo%20(11).png" alt="DevCraft" class="devcraft-logo" />
        <span class="devcraft-text">Desarrollado por <span>DevCraft</span></span>
      </a>
    </div>
  </div>

</footer>

<script>
  // Dynamic year
  document.querySelectorAll('.js-year').forEach(el => {
    (el as HTMLElement).textContent = new Date().getFullYear().toString();
  });
</script>