---
// About Section - Quiénes Somos
---

<section class="about-section" id="nosotros">
  <!-- Logo watermark -->
  <div class="about-watermark" aria-hidden="true">
    <img src="/logo/logo2.svg" alt="" />
  </div>

  <!-- Subtle diagonal stripe texture -->
  <div class="about-texture"></div>

  <div class="about-container">
    <!-- Grid -->
    <div class="about-grid">

      <!-- TEXT COLUMN -->
      <div class="about-text-col animate-on-scroll">
        <!-- Eyebrow -->
        <div class="about-eyebrow">
          <span class="about-eyebrow-rule"></span>
          <span class="about-eyebrow-label">Nuestra historia</span>
        </div>

        <h2 class="about-title">Quiénes<br/><em>Somos</em></h2>
        <div class="about-accent-line"></div>

        <div class="about-body">
          <p>
            <strong>S3A Industrial</strong> es una empresa especializada en el servicio de
            <strong class="about-highlight">reparación y mantenimiento de sopladores industriales</strong>,
            con años de experiencia brindando soluciones técnicas de alta calidad a grandes plantas e industrias.
          </p>
          <p>
            Nuestro equipo de profesionales altamente capacitados se traslada directamente
            a su planta para ejecutar cada intervención, minimizando los tiempos de inactividad
            y asegurando la continuidad operativa de su empresa.
          </p>
          <p>
            Trabajamos con todas las marcas y modelos de sopladores industriales,
            ofreciendo diagnósticos precisos, reparaciones integrales y planes de
            mantenimiento preventivo adaptados a sus necesidades.
          </p>
        </div>

        <!-- Features grid -->
        <div class="about-features">
          <div class="feature-card">
            <div class="feature-icon">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
              </svg>
            </div>
            <span>Experiencia Comprobada</span>
          </div>
          <div class="feature-card">
            <div class="feature-icon">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
              </svg>
            </div>
            <span>Respuesta Rápida</span>
          </div>
          <div class="feature-card">
            <div class="feature-icon">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6V4m0 2a2 2 0 100 4m0-4a2 2 0 110 4m-6 8a2 2 0 100-4m0 4a2 2 0 110-4m0 4v2m0-6V4m6 6v10m6-2a2 2 0 100-4m0 4a2 2 0 110-4m0 4v2m0-6V4"></path>
              </svg>
            </div>
            <span>Servicio en Sitio</span>
          </div>
          <div class="feature-card">
            <div class="feature-icon">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4M7.835 4.697a3.42 3.42 0 001.946-.806 3.42 3.42 0 014.438 0 3.42 3.42 0 001.946.806 3.42 3.42 0 013.138 3.138 3.42 3.42 0 00.806 1.946 3.42 3.42 0 010 4.438 3.42 3.42 0 00-.806 1.946 3.42 3.42 0 01-3.138 3.138 3.42 3.42 0 00-1.946.806 3.42 3.42 0 01-4.438 0 3.42 3.42 0 00-1.946-.806 3.42 3.42 0 01-3.138-3.138 3.42 3.42 0 00-.806-1.946 3.42 3.42 0 010-4.438 3.42 3.42 0 00.806-1.946 3.42 3.42 0 013.138-3.138z"></path>
              </svg>
            </div>
            <span>Calidad Garantizada</span>
          </div>
        </div>
      </div>

      <!-- IMAGE COLUMN -->
      <div class="about-img-col animate-on-scroll">
        <div class="about-image-wrapper">
          <img
            src="/imagenes/S3AImagnes/repicky27.png"
            alt="Equipo S3A Industrial trabajando"
            class="about-img"
            loading="lazy"
          />
          <!-- Badge flotante -->
          <div class="about-badge">
            <span class="about-badge-num">10+</span>
            <span class="about-badge-txt">Años de Experiencia</span>
          </div>
          <!-- Decorative border frame -->
          <div class="about-img-frame"></div>
        </div>
      </div>
    </div>

    <!-- DIVIDER -->
    <div class="about-divider"></div>

    <!-- SECOND ROW: Compromiso -->
    <div class="about-grid about-grid--reverse">

      <!-- IMAGE COLUMN (left on desktop) -->
      <div class="about-img-col about-img-col--secondary animate-on-scroll">
        <div class="about-image-wrapper">
          <img
            src="/imagenes/S3AImagnes/repicky6.png"
            alt="Técnicos S3A Industrial en planta"
            class="about-img"
            loading="lazy"
          />
          <div class="about-img-frame"></div>
          <!-- Stat badge right side -->
          <div class="about-badge about-badge--right">
            <span class="about-badge-num">100%</span>
            <span class="about-badge-txt">Compromiso Técnico</span>
          </div>
        </div>
      </div>

      <!-- TEXT COLUMN (right on desktop) -->
      <div class="about-text-col about-text-col--secondary animate-on-scroll">
        <div class="about-eyebrow">
          <span class="about-eyebrow-rule"></span>
          <span class="about-eyebrow-label">Nuestro compromiso</span>
        </div>

        <h2 class="about-title about-title--secondary">Trabajo<br/><em>de Campo</em></h2>
        <div class="about-accent-line"></div>

        <div class="about-body">
          <p>
            Cada intervención es realizada con los <strong>más altos estándares de seguridad y precisión técnica</strong>.
            Nuestros especialistas llegan equipados con herramientas de diagnóstico de última generación
            para identificar fallas con exactitud desde el primer momento.
          </p>
          <p>
            Sabemos que cada hora de parada representa un costo real para su operación.
            Por eso nos movilizamos con precisión y rigor técnico, sin resignar calidad, garantizando
            <strong class="about-highlight">resultados duraderos en cada reparación</strong>.
          </p>
        </div>

        <!-- Stats row -->
        <div class="about-stats">
          <div class="stat-item">
            <span class="stat-num">+100</span>
            <span class="stat-label">Equipos Reparados</span>
          </div>
          <div class="stat-divider"></div>
          <div class="stat-item">
            <span class="stat-num">Móvil</span>
            <span class="stat-label">Vamos a su Planta</span>
          </div>
          <div class="stat-divider"></div>
          <div class="stat-item">
            <span class="stat-num">+50</span>
            <span class="stat-label">Plantas Atendidas</span>
          </div>
        </div>
      </div>
    </div>

  </div>
</section>

<style>
  /* ============================================
     ABOUT SECTION
  ============================================ */
  .about-section {
    position: relative;
    padding: 100px 0 80px;
    overflow: visible;
    background: linear-gradient(160deg, #f7f4ef 0%, #f0ece4 50%, #f4f1ec 100%);
  }

  .about-watermark {
    position: absolute;
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    pointer-events: none;
    opacity: 0.035;
    z-index: 0;
  }
  .about-watermark img { width: 600px; height: 600px; object-fit: contain; }

  .about-texture {
    position: absolute;
    inset: 0;
    background-image: repeating-linear-gradient(
      45deg,
      transparent,
      transparent 20px,
      rgba(49, 61, 73, 0.018) 20px,
      rgba(49, 61, 73, 0.018) 40px
    );
    pointer-events: none;
  }

  .about-container {
    max-width: 1240px;
    margin: 0 auto;
    padding: 0 32px;
    position: relative;
    z-index: 2;
    overflow: visible;
  }
  @media (max-width: 640px) { .about-container { padding: 0 20px; } }

  /* -- GRID -- */
  .about-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 72px;
    align-items: center;
    margin-bottom: 0;
    overflow: visible;
  }
  @media (max-width: 960px) {
    .about-grid { grid-template-columns: 1fr; gap: 48px; }
  }

  /* -- TEXT COLUMN -- */
  .about-text-col { order: 1; overflow: visible; padding-right: 0.15em; }
  @media (max-width: 960px) { .about-text-col { order: 2; text-align: center; } }

  .about-eyebrow {
    display: flex;
    align-items: center;
    gap: 12px;
    margin-bottom: 20px;
  }
  @media (max-width: 960px) { .about-eyebrow { justify-content: center; } }

  .about-eyebrow-rule {
    width: 32px;
    height: 2px;
    background: linear-gradient(90deg, transparent, #334c60);
    border-radius: 2px;
    flex-shrink: 0;
  }
  .about-eyebrow-label {
    font-family: var(--font-heading);
    font-size: 0.72rem;
    font-weight: 700;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: #223546;
  }

  .about-title {
    font-family: var(--font-display);
    font-size: clamp(3.2rem, 6vw, 5.2rem);
    font-weight: 400;
    line-height: 1.05;
    color: #1e313f;
    letter-spacing: 0.02em;
    text-transform: uppercase;
    margin-bottom: 20px;
    overflow: visible;
    padding-right: 0.1em;
  }
  .about-title em {
    font-style: italic;
    display: inline-block;
    padding-right: 0.12em;
    background: linear-gradient(135deg, #334c60, #223546);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .about-accent-line {
    width: 72px;
    height: 4px;
    background: linear-gradient(90deg, #1e313f, #334c60, #223546);
    border-radius: 2px;
    margin-bottom: 28px;
    position: relative;
  }
  .about-accent-line::after {
    content: '';
    position: absolute;
    left: 0;
    top: 8px;
    width: 44px;
    height: 2px;
    background: rgba(51, 76, 96, 0.3);
    border-radius: 2px;
  }
  @media (max-width: 960px) {
    .about-accent-line { margin-left: auto; margin-right: auto; }
  }

  .about-body {
    font-family: var(--font-body);
    font-size: clamp(0.95rem, 1.4vw, 1.05rem);
    line-height: 1.85;
    color: rgba(34, 53, 70, 0.82);
    margin-bottom: 32px;
  }
  .about-body p { margin-bottom: 1rem; }
  .about-body p:last-child { margin-bottom: 0; }
  .about-body strong { color: #1e313f; font-weight: 700; }
  .about-highlight { color: #223546; font-weight: 700; font-style: normal; }

  /* -- FEATURE CARDS -- */
  .about-features {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 12px;
  }
  @media (max-width: 480px) { .about-features { grid-template-columns: 1fr; } }

  .feature-card {
    display: flex;
    align-items: center;
    gap: 12px;
    padding: 14px 16px;
    background: #fff;
    border-radius: 12px;
    border: 1px solid rgba(34, 53, 70, 0.1);
    box-shadow: 0 2px 12px rgba(0, 0, 0, 0.05);
    font-family: var(--font-heading);
    font-size: 0.85rem;
    font-weight: 700;
    color: #1e313f;
    text-transform: uppercase;
    letter-spacing: 0.04em;
    transition: all 0.3s cubic-bezier(0.22, 1, 0.36, 1);
  }
  .feature-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 10px 28px rgba(30, 49, 63, 0.13);
    border-color: rgba(34, 53, 70, 0.25);
  }

  .feature-icon {
    width: 38px;
    height: 38px;
    border-radius: 10px;
    background: linear-gradient(135deg, #1e313f, #334c60);
    display: flex;
    align-items: center;
    justify-content: center;
    color: #fff;
    flex-shrink: 0;
    transition: transform 0.3s ease;
  }
  .feature-card:hover .feature-icon { transform: scale(1.1) rotate(5deg); }

  /* -- IMAGE COLUMN -- */
  .about-img-col { order: 2; }
  @media (max-width: 960px) { .about-img-col { order: 1; } }

  .about-image-wrapper { position: relative; }

  .about-img {
    width: 100%;
    border-radius: 20px;
    box-shadow: 0 24px 64px rgba(0, 0, 0, 0.18);
    display: block;
  }

  .about-img-frame {
    position: absolute;
    inset: -12px;
    border: 2px solid rgba(34, 53, 70, 0.18);
    border-radius: 24px;
    pointer-events: none;
    z-index: -1;
  }

  .about-badge {
    position: absolute;
    bottom: -18px;
    left: -18px;
    background: linear-gradient(135deg, #1e313f, #334c60);
    padding: 20px 24px;
    border-radius: 18px;
    box-shadow: 0 14px 36px rgba(30, 49, 63, 0.45);
    display: flex;
    flex-direction: column;
    align-items: center;
    z-index: 2;
  }
  @media (max-width: 640px) {
    .about-badge { bottom: -12px; left: -8px; padding: 14px 18px; }
  }
  .about-badge-num {
    font-family: var(--font-display);
    font-size: 2.8rem;
    font-weight: 400;
    color: #fff;
    line-height: 1;
    letter-spacing: 0.02em;
  }
  .about-badge-txt {
    font-family: var(--font-heading);
    font-size: 0.68rem;
    font-weight: 700;
    color: rgba(255, 255, 255, 0.8);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    margin-top: 4px;
    text-align: center;
  }



  /* -- SCROLL ANIMATION -- */
  .animate-on-scroll {
    opacity: 0;
    transform: translateY(24px);
    transition: opacity 0.7s cubic-bezier(0.22, 1, 0.36, 1), transform 0.7s cubic-bezier(0.22, 1, 0.36, 1);
  }
  .animate-on-scroll.is-visible {
    opacity: 1;
    transform: translateY(0);
  }
  .about-img-col.animate-on-scroll { transition-delay: 0.15s; }
  .about-text-col--secondary.animate-on-scroll { transition-delay: 0.15s; }

  /* -- DIVIDER -- */
  .about-divider {
    width: 100%;
    height: 1px;
    background: linear-gradient(90deg, transparent, rgba(34,53,70,0.15), transparent);
    margin: 80px 0;
  }

  /* -- SECOND ROW -- */
  .about-grid--reverse .about-img-col--secondary { order: 1; }
  .about-grid--reverse .about-text-col--secondary { order: 2; }
  @media (max-width: 960px) {
    .about-grid--reverse .about-img-col--secondary { order: 1; }
    .about-grid--reverse .about-text-col--secondary { order: 2; text-align: center; }
  }

  .about-title--secondary { font-size: clamp(2.8rem, 5vw, 4.4rem); }

  .about-badge--right {
    bottom: -18px;
    left: auto;
    right: -18px;
  }
  @media (max-width: 640px) {
    .about-badge--right { bottom: -12px; right: -8px; left: auto; }
  }

  /* -- STATS ROW -- */
  .about-stats {
    display: flex;
    align-items: center;
    gap: 0;
    margin-top: 36px;
    background: #fff;
    border-radius: 16px;
    border: 1px solid rgba(34,53,70,0.1);
    box-shadow: 0 4px 20px rgba(0,0,0,0.06);
    overflow: hidden;
  }
  @media (max-width: 960px) { .about-stats { justify-content: center; } }
  @media (max-width: 480px) { .about-stats { flex-direction: column; } }

  .stat-item {
    flex: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: 22px 16px;
    gap: 4px;
  }
  .stat-num {
    font-family: var(--font-display);
    font-size: clamp(1.8rem, 3vw, 2.4rem);
    font-weight: 400;
    color: #1e313f;
    letter-spacing: 0.02em;
    line-height: 1;
  }
  .stat-label {
    font-family: var(--font-heading);
    font-size: 0.68rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    color: rgba(34,53,70,0.55);
    text-align: center;
  }
  .stat-divider {
    width: 1px;
    height: 48px;
    background: rgba(34,53,70,0.12);
    flex-shrink: 0;
  }
  @media (max-width: 480px) {
    .stat-divider { width: 80%; height: 1px; }
  }
</style>

<script>
  const observer = new IntersectionObserver(
    (entries) => entries.forEach(e => { if (e.isIntersecting) e.target.classList.add('is-visible'); }),
    { threshold: 0.12 }
  );
  document.querySelectorAll('.animate-on-scroll').forEach(el => observer.observe(el));
</script>